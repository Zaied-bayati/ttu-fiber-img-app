<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Ttu.FiberImgApp.Windows.FullscreenImageWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Image viewer">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Padding="12,8" Background="#CC111111">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="TitleText"
                VerticalAlignment="Center"
                Foreground="White"
                Text="Fullscreen — drag to pan, scroll wheel to zoom"
                TextWrapping="NoWrap" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Content="Zoom −" Click="ZoomOut_Click" />
                <Button Content="Zoom +" Click="ZoomIn_Click" />
                <Button Content="Reset" Click="ZoomReset_Click" />
                <Button Content="Close (Esc)" Click="Close_Click" />
            </StackPanel>
        </Grid>

        <ScrollViewer
            x:Name="ZoomViewer"
            Grid.Row="1"
            ZoomMode="Enabled"
            MinZoomFactor="0.25"
            MaxZoomFactor="8"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto"
            Background="Black"
            PointerPressed="ZoomViewer_PointerPressed"
            PointerMoved="ZoomViewer_PointerMoved"
            PointerReleased="ZoomViewer_PointerReleased"
            PointerCanceled="ZoomViewer_PointerCanceled"
            PointerCaptureLost="ZoomViewer_PointerCaptureLost">
            <Image
                x:Name="ViewerImage"
                Stretch="Uniform"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />
        </ScrollViewer>
    </Grid>
</Window>