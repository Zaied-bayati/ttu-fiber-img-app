<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Ttu.FiberImgApp.Pages.ActivityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Serial monitor" Style="{StaticResource TitleTextBlockStyle}" />
        <TextBlock
            Grid.Row="1"
            Margin="0,4,0,12"
            Opacity="0.75"
            TextWrapping="WrapWholeWords"
            Text="Step-by-step status for camera, stage, and sampling. Select text or use Copy all to paste into chat." />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <Button Content="Copy all" Click="CopyLog_Click" />
            <Button Content="Clear" Click="ClearLog_Click" />
        </StackPanel>

        <TextBox
            x:Name="ActivityLogBox"
            Grid.Row="3"
            AcceptsReturn="True"
            IsReadOnly="True"
            IsSpellCheckEnabled="False"
            TextWrapping="NoWrap"
            FontFamily="Consolas"
            FontSize="12"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            PlaceholderText="Connect camera or stage to see activity here..." />
    </Grid>
</Page>
